<div class="row mb-5">
  {{!-- 左側老師資訊 --}}
  <div class="col-md-4">

    {{!-- 頭像 --}}
    <div class="tutor-avatar mb-5">
      {{#if user.avatar}}
      <img class="avatar" src="{{user.avatar}}" alt="avatar" title="avatar">
      {{else}}
      <img class="default-avatar" src="/stylesheets/icons/default-avatar.svg" alt="avatar" title="avatar">
      {{/if}}
    </div>

    <div class="tutor-info mb-5">
      {{!-- 名字 --}}
      <h3>{{user.name}}</h3>
      {{!-- 國家 --}}
      <p>{{user.nation}}</p>
      {{!-- 評分 --}}
      <div>
        <i class="fa-solid fa-star" style="color: #e4b02b;"></i>
        {{#if avgRating}}
        <strong class="course-rating badge">{{avgRating}}</strong>
        {{else}}
        <strong style="font-size: 12px;">尚未有評價</strong>
        {{/if}}
      </div>
      {{!-- 自我介紹 --}}
      <div class="mt-5">
        <strong>關於我</strong>
        <p style="white-space: break-spaces;">{{user.Tutor.tutorIntroduction}}</p>
      </div>
    </div>

    {{!-- 編輯按鈕 --}}
    <a class="custom-solid-button" href="/tutor/{{user.id}}/edit" style="text-decoration: none;">編輯課程資訊與時間</a>
  </div>

  {{!-- 右側 --}}
  <div class="col-md-8">
    {{!-- 最新課程 --}}
    <h4>即將到來</h4>
    <div class="row mb-5">
      {{#each futureCourses}}
      <div class="col-md-6 col-12" style="height: 100px; margin: 10px 0px">
        <div class="card mb-4 shadow-sm h-100" id="course-card">

          {{!-- 學生頭像 --}}
          <div class="d-flex align-items-center m-2">
            <div class="user-avatar mb-2">
              {{#if this.User.avatar}}
              <img class="avatar" src="{{this.User.avatar}}" alt="avatar" title="avatar">
              {{else}}
              <img class="default-avatar" src="/stylesheets/icons/default-avatar.svg" alt="avatar" title="avatar">
              {{/if}}
            </div>
          </div>

          <div class="course-info m-2">
            {{!-- 學生名字 --}}
            <span class="user-name">{{this.User.name}}</span>
            {{!-- 課程時間 --}}
            <span>{{this.time}}</span>
          </div>

          {{!-- 課程連結 --}}
          <div class="d-flex align-items-center mx-3">
            <a class="custom-solid-button " style="font-size: 15px; padding: 6px; color: white"
              href="{{this.Tutor.teachingLink}}">進入教室</a>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
    <hr>

    {{!-- 教學風格 --}}
    <div class="mb-5" id="teaching-style">
      <h4>教學風格</h4>
      <p class="mt-4">{{user.Tutor.teachingStyle}}</p>
    </div>
    <hr>

    {{!-- 學生評論 --}}
    <div id="recent-reviewed">
      <h4>最新評論</h4>
      <div class="mt-4">
        {{#each ratedCourses}}
        <div class="class-info m-2">
          <i class="fa-solid fa-star" style="color: #e4b02b;"></i>
          <strong class="course-rating badge" style="width: 30px">{{this.rating}}</strong>
          ：
          <span class="course-comment">{{#if this.comment}}{{this.comment}}{{else}}沒有留下評論{{/if}}</span>
        </div>
        {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>